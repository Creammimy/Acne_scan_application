acne_backend/
├── best.pt                # ไฟล์โมเดล YOLO ที่ผ่านการฝึกแล้ว
├── main.py                # ไฟล์หลักสำหรับรัน backend 
├── requirements.txt       # รายชื่อ dependencies ที่ต้องติดตั้ง
└── run backend.txt        # ไฟล์คำสั่งการรัน backend
